spring:
  application:
    name: taskManager

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.yaml

  mvc:
    hiddenmethod:
      filter:
        enabled: true

logging:
  level:
    root: INFO
rollbar_token: 123212